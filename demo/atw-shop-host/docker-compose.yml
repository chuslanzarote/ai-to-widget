# docker-compose.yml
#
# The canonical reviewer compose lives at the repo root — this file is
# the Feature 002 activation anchor and is left intentionally empty
# between the atw:begin/atw:end markers. /atw.build's COMPOSE step
# reports `unchanged` against it. A reviewer brings the full stack up
# with `docker compose up -d --wait` from the repo root, not from here.

# ----- atw:begin -----
# ----- atw:end -----
