# Schema Map

## Summary

Generated by `/atw.schema` on 2026-04-24 from `demo/shop/backend/prisma/migrations/20260423000000_init/migration.sql`. Inspected 6 tables total. One entity (`Products`) is RAG-indexable; `Cart`, `Orders`, `Customers` are declared as `infrastructure` so the validator recognises tool groups that target them, but enrichment and indexing skip them — they are accessed exclusively via widget-executed tools with the shopper's bearer token (Feature 007). `users` is PII-excluded.

Temporary patch note: the three `infrastructure`-classified entity stubs exist only so the Feature 006 validator rule *"every action-manifest tool group has a schema-map entity"* passes. Once the classifier/validator is refactored for Feature 007 (planned follow-up task), those stubs can be removed.

## Entity: Products

Classification: indexable
Source tables: products
Joined references: *(none)*

### Columns

- `id`: index — UUID primary key; used for tool calls and links.
- `handle`: index — unique URL slug.
- `name`: index — primary product name.
- `description`: index — full description; primary semantic signal.
- `price_cents`: index — enables price-aware comparison.
- `image_url`: index — rendered by the widget.
- `in_stock`: index — stock awareness signal.
- `created_at`: exclude-internal — not user-facing.

### Evidence

Brief `allowed actions` states the agent must "list, compare and understand products". Columns are all customer-facing catalogue data with no PII signals.

## Entity: Cart

Classification: infrastructure
Source tables: carts, cart_items
Joined references: *(none)*

### Columns

- *(no columns classified)*

### Evidence

Per-shopper runtime state. Never indexed — accessed via widget-executed tools (`get_cart`, `add_cart_item`, `update_cart_item`, `remove_cart_item`) with the shopper's bearer token.

## Entity: Orders

Classification: infrastructure
Source tables: orders, order_items
Joined references: *(none)*

### Columns

- *(no columns classified)*

### Evidence

Per-shopper transactional history. Never indexed — indexing would cross-leak between shoppers. Accessed via widget-executed tool (`list_my_orders`) with the shopper's bearer token. `POST /orders` (place order) is brief-forbidden and excluded from the action manifest.

## Entity: Customers

Classification: infrastructure
Source tables: users
Joined references: *(none)*

### Columns

- *(no columns classified)*

### Evidence

Shopper profile. Source table `users` is separately PII-excluded. Accessed via widget-executed tool (`get_my_profile`) with the shopper's bearer token.

## Reference tables

- *(none)*

## Infrastructure / ignored

- carts
- cart_items
- orders
- order_items

## PII-excluded

- users: columns [email, password_hash, display_name] — analog of `customer`; excluded wholesale per FR-022.
