{
  "name": "@atw/backend",
  "version": "0.1.0",
  "description": "Ai to Widget backend — Handlebars templates for the runtime service + multi-stage Dockerfile. Feature 003 fills runtime behavior; this package ships compiling stubs.",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "node -e \"console.log('[@atw/backend] skipping host-side build — rendered inside /atw.build + Dockerfile (templates are .ts.hbs)')\"",
    "build:ci": "tsc -b",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "@fastify/cors": "^9.0.1",
    "@fastify/rate-limit": "^9.1.0",
    "@types/debug": "^4.1.12",
    "@types/node": "^20.11.0",
    "@xenova/transformers": "^2.17.0",
    "debug": "^4.3.4",
    "fastify": "^4.28.0",
    "fastify-type-provider-zod": "^2.0.0",
    "pg": "^8.11.3",
    "pino": "^9.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "pino-pretty": "^11.0.0"
  }
}
